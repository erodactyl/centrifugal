<style>
  :global(button),
  :global(button:focus) {
    margin: 0;
    outline: none;
    background-color: #06bcc1;
    cursor: pointer;
    color: #fff;
    padding: 8px 20px;
    border-radius: 5px;
  }

  :global(button:hover) {
    background-color: #05999e;
  }

  :global(.alias) {
    font-size: 18px;
    font-weight: 800;
  }

  :global(input:focus),
  :global(textarea:focus),
  :global(select:focus) {
    outline: none;
  }

  :global(input) {
    border-radius: 5px;
    padding: 15px;
  }

  :global(input:focus) {
    border: 1px solid #06bcc1;
  }
</style>
